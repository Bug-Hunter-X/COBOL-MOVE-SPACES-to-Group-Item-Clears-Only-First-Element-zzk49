MOVE SPACES TO WS-OUTPUT-AREA-ITEM1.
MOVE SPACES TO WS-OUTPUT-AREA-ITEM2.
MOVE SPACES TO WS-OUTPUT-AREA-ITEM3.
* ... and so on for all items in the group

*Alternatively, use PERFORM VARYING to loop through each element
PERFORM VARYING I FROM 1 BY 1 UNTIL I > NUMBER-OF-ITEMS
    MOVE SPACES TO WS-OUTPUT-AREA(I)
END-PERFORM.